# Project Instructions fo codex

## Testing

On Linux, unittest commands should start with `python3 -m unittest `.

Example:

```bash
python3 -m unittest app.leetcode.lc_806_number_of_lines_to_write_string.test_algorithm
```
